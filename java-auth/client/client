#!/usr/bin/env bash

# Simple wrapper to run the Java Auth Client
cd "$(dirname "$0")"

echo "Building Java Auth Client..."
mvn compile -q

echo "Starting Java Auth Client..."
mvn exec:java -q