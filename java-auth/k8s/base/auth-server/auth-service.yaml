apiVersion: v1
kind: Service
metadata:
  name: java-auth-service
  labels:
    app: java-auth
spec:
  selector:
    app: java-auth
  ports:
  - name: http
    port: 80
    targetPort: 8081
    protocol: TCP
  type: ClusterIP
  sessionAffinity: None