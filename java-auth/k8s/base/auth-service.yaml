apiVersion: v1
kind: Service
metadata:
  name: java-auth-service
  namespace: java-auth
  labels:
    app: java-auth
spec:
  selector:
    app: java-auth
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
  sessionAffinity: None