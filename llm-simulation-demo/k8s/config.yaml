apiVersion: v1
kind: ConfigMap
metadata:
  name: llm-simulation-config
  namespace: llm-simulation
data:
  DEFAULT_PROVIDER: "openai"
  ENABLED_PROVIDERS: "openai,anthropic,gemini,xai"
  TOOL_BASE_URL: "http://llm-simulation-tools"
